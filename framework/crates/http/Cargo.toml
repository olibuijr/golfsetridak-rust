[package]
name = "akurai-http"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Pure-std HTTP/1.1 core for AkurAI-Framework. Zero dependencies."

[lib]
path = "src/lib.rs"

# No [dependencies] — std only, by design. See AGENTS.md principle #1.
